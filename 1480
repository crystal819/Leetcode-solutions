class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        run_sum_arr = [nums[0]]
        for i in range(1, len(nums)):
            run_sum_arr.append(run_sum_arr[-1] + nums[i])
        return run_sum_arr

        
