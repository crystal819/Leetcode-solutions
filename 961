class Solution:
    def repeatedNTimes(self, nums: list[int]) -> int:
        n = len(nums)
        for i in range(n):
            for j in range(n-1, -1, -1):
                if j != i and nums[i] == nums[j]:
                    return nums[i]
